/**
 * Rainbow admin portal
 * # Rainbow administrator portal API guide  ## Preamble  ### Introduction  This guide describes list of API services that are provided by OT Rainbow management portal system. Services are used to manage OT Rainbow system entities  ### Protocol  REST interface is used for sending/receiving OT rainbow API messages. HTTP requests GET, DELETE, POST, UPDATE are used. Standard HTTP responses are used to provide requested information or error status. There is no session notion in OT Rainbow system, so requests could be issued according stateless model, without transport conservation between them. Additional data could be provided in message body. JSON is used as a main format for data encoding in message body part. Each request is started with the following pattern /{module}/{version}/ where {module} is a portal module name to address and {version} is a version of used API, par example, “v1.0”.  ### Security considerations  Each request should contain some credential information to authenticate itself. Standard HTTP authentication with basic/bearer modes is used. JSON Web Token mechanism is used to provide authentication information. JWT has a expire timeout that is controlled by OT Rainbow portal to prevent very long token usage. Also authentication with application token is used. The token must be provided in the request HTTP header, using a custom header: APIKey. At server side, token is verified, and if it doesn’t match, 403 Not Allowed response is sent. TLS is used as a transport protocol to support message exchanges between OT Rainbow portal and an application.  
 *
 * OpenAPI spec version: 1.82.0
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 *
 * OpenAPI Generator version: 4.0.0-beta3
 *
 * Do not edit the class manually.
 *
 */

(function(root, factory) {
  if (typeof define === 'function' && define.amd) {
    // AMD. Register as an anonymous module.
    define(['ApiClient', 'model/PostSystemsRestPasswordSuccess'], factory);
  } else if (typeof module === 'object' && module.exports) {
    // CommonJS-like environments that support module.exports, like Node.
    module.exports = factory(require('../ApiClient'), require('../model/PostSystemsRestPasswordSuccess'));
  } else {
    // Browser globals (root is window)
    if (!root.RainbowAdminPortal) {
      root.RainbowAdminPortal = {};
    }
    root.RainbowAdminPortal.SystemsResetPasswordApi = factory(root.RainbowAdminPortal.ApiClient, root.RainbowAdminPortal.PostSystemsRestPasswordSuccess);
  }
}(this, function(ApiClient, PostSystemsRestPasswordSuccess) {
  'use strict';

  /**
   * SystemsResetPassword service.
   * @module api/SystemsResetPasswordApi
   * @version 1.82.0
   */

  /**
   * Constructs a new SystemsResetPasswordApi. 
   * @alias module:api/SystemsResetPasswordApi
   * @class
   * @param {module:ApiClient} [apiClient] Optional API client implementation to use,
   * default to {@link module:ApiClient#instance} if unspecified.
   */
  var exports = function(apiClient) {
    this.apiClient = apiClient || ApiClient.instance;



    /**
     * Reset CCCA jid password
     * It would be necessary to reset the password used by the CCCA to join rainbow infrastructure.    Among system data fields, jid_pbxagent and jid_pbxagent_password are used by the CCCA to try an authentication.    The new API :   * regenerate an activation code which is stored in the Admin portal and XMPP server (four digits) * does not change the existing pbxId * replace the system status from \&quot;activated\&quot; to \&quot;created\&quot;     To finalize CCCA authentication refer to (Get a pbx data using API [GET /api/rainbow/pcg/v1.0/pbxs/{pbxId}][0]).  [0]: #api-pcg_pbxs-GetPbxId
     * @param {String} systemId System unique identifier (like 569ce8c8f9336c471b98eda1)
     * @return {Promise} a {@link https://www.promisejs.org/|Promise}, with an object containing data of type {@link module:model/PostSystemsRestPasswordSuccess} and HTTP response
     */
    this.postSystemsRestPasswordWithHttpInfo = function(systemId) {
      var postBody = null;

      // verify the required parameter 'systemId' is set
      if (systemId === undefined || systemId === null) {
        throw new Error("Missing the required parameter 'systemId' when calling postSystemsRestPassword");
      }


      var pathParams = {
        'systemId': systemId
      };
      var queryParams = {
      };
      var collectionQueryParams = {
      };
      var headerParams = {
      };
      var formParams = {
      };

      var authNames = ['Bearer'];
      var contentTypes = [];
      var accepts = ['application/json'];
      var returnType = PostSystemsRestPasswordSuccess;
      return this.apiClient.callApi(
        '/api/rainbow/admin/v1.0/systems/{systemId}/reset-password', 'POST',
        pathParams, queryParams, collectionQueryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null
      );
    }

    /**
     * Reset CCCA jid password
     * It would be necessary to reset the password used by the CCCA to join rainbow infrastructure.    Among system data fields, jid_pbxagent and jid_pbxagent_password are used by the CCCA to try an authentication.    The new API :   * regenerate an activation code which is stored in the Admin portal and XMPP server (four digits) * does not change the existing pbxId * replace the system status from \&quot;activated\&quot; to \&quot;created\&quot;     To finalize CCCA authentication refer to (Get a pbx data using API [GET /api/rainbow/pcg/v1.0/pbxs/{pbxId}][0]).  [0]: #api-pcg_pbxs-GetPbxId
     * @param {String} systemId System unique identifier (like 569ce8c8f9336c471b98eda1)
     * @return {Promise} a {@link https://www.promisejs.org/|Promise}, with data of type {@link module:model/PostSystemsRestPasswordSuccess}
     */
    this.postSystemsRestPassword = function(systemId) {
      return this.postSystemsRestPasswordWithHttpInfo(systemId)
        .then(function(response_and_data) {
          return response_and_data.data;
        });
    }
  };

  return exports;
}));
